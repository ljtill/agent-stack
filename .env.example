# ── Application ──────────────────────────────────────────────────────
APP_ENV=development
APP_SECRET_KEY=
LOG_LEVEL=INFO

# ── Microsoft Foundry ────────────────────────────────────────────────
# Project endpoint for LLM inference and memory.
# Create a project at https://foundry.microsoft.com and deploy a model.
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_MODEL=your-model-deployment-name

# Agent memory (optional — disabled when FOUNDRY_PROJECT_ENDPOINT is blank)
FOUNDRY_EMBEDDING_MODEL=text-embedding-3-small
FOUNDRY_CHAT_MODEL=gpt-4.1-mini
FOUNDRY_MEMORY_STORE_NAME=editorial-memory
# FOUNDRY_MEMORY_UPDATE_DELAY=60
# FOUNDRY_MEMORY_ENABLED=true

# ── Microsoft Entra ID ──────────────────────────────────────────────
# Single-tenant app registration for editorial dashboard authentication.
ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_REDIRECT_URI=http://localhost:8000/auth/callback

# ── Azure Services ──────────────────────────────────────────────────
# Cosmos DB — document store and change feed event source.
# Use http://localhost:8081 for the local emulator (docker compose up -d).
AZURE_COSMOS_ENDPOINT=http://localhost:8081
AZURE_COSMOS_DATABASE=agent-stack

# Blob Storage — static newsletter site hosting.
# Use the Azurite defaults below for local development.
AZURE_STORAGE_ACCOUNT_URL=http://127.0.0.1:10000/devstoreaccount1
AZURE_STORAGE_CONTAINER=$$web

# App Configuration — runtime settings in deployed environments (optional locally).
AZURE_APP_CONFIG_ENDPOINT=

# Application Insights — telemetry and monitoring (optional locally).
AZURE_APPLICATIONINSIGHTS_CONNECTION_STRING=

# ── Rate Limiting (optional, defaults shown) ────────────────────────
# OPENAI_TPM_LIMIT=800000
# OPENAI_RPM_LIMIT=8000
